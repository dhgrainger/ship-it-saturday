<!DOCTYPE html>

<head>
  <title>Krash Orders!</title>
     <link rel="stylesheet" href="shipit.css">
  </head>

  <body>

    <h1>Krash Orders</h1>


    <table>
     <tr>
      <th>Unit</th>
       <th>Ordered By</th>
      <th colspan="3">Items</th>


      <% @orders.each do |order| %>

         <tr>
          <td><%= order[:house] %></td>
          <td><%= order[:name] %></td>
          <td><%= order[:items].map {|i| i.to_s}.join(" ") %></td>
          </tr>

      <% end %>
    </table>
  </body>
</html>
